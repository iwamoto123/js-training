import { TrainingSkipError } from "./common/TrainingSkipError.js";
// No. 31 棒グラフ改
// 整数値を入力し、その個数だけ `*` を表示します。
// ただし、5個ごとに空白（スペース）を挿入します。
// 入力値が0以下の場合は何も出力しません。

// 実行例:
//   入力:
//     数値: 7
//   出力:
//     ***** **
//   入力:
//     数値: 13
//   出力:
//     ***** ***** ***

// [Tips]
// - このトレーニングでは、関数の引数として整数値を受け取ります。
// - 繰り返し処理を使い、出力結果を組み立てる方法を学びます。
// - 空白を5個ごとに入れるためには、現在の位置をカウントする必要があります。
// - 配列を使って部分的な文字列をまとめ、最後に結合する方法も活用できます。

/**
 * 入力された値に応じて棒グラフを作成する関数
 * @param {number} n - 入力された整数値
 * @returns {string} - 棒グラフ（`*`をn個並べた文字列）。5個ごとにスペースを挿入。
 */
export const knock = (n = 7) => {
  throw new TrainingSkipError("未実装");
};
